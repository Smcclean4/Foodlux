import React from 'react';

const Finedine = (props) => {
  return (
   <div>
    {props.logo}
    <br />
    {props.title}
    <br />
    {props.description}
   </div>
  );
}

export default Finedine